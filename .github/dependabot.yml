# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://docs.github.com/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file

version: 2
updates:
  # Enable version updates for npm
  - package-ecosystem: "npm"
    # Look for `package.json` and `lock` files in the `root` directory
    directory: "/"
    # Check the npm registry for updates once every week on a weekday
    schedule:
      interval: "weekly"
    ignore:
      # Internal dependencies that we update manually
      - dependency-name: "pprof-format"
      # TypeScript 7 (the native port) is not yet supported by typescript-eslint
      # (and therefore gts), so major bumps break `npm run lint`.
      # See https://github.com/typescript-eslint/typescript-eslint/issues/10940
      # Re-enable once typescript-eslint ships TS >=7 support.
      - dependency-name: "typescript"
        update-types: ["version-update:semver-major"]
      # mocha 12 is ESM-only and requires Node ^20.19 || >=22.12, but we still
      # build and test on Node 18. Its lib/cli/options.cjs require()s an ESM
      # module, which fails with ERR_REQUIRE_ESM on Node 18.
      # Re-enable once we drop Node 18 support.
      - dependency-name: "mocha"
        update-types: ["version-update:semver-major"]
    versioning-strategy: "increase"
    labels:
      - dependabot
      - dependencies
      - javascript
      - semver-patch
    groups:
      patch-updates:
        update-types:
          - "patch"
      minor-updates:
        update-types:
          - "minor"
